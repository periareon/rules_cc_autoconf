"""https://github.com/coreutils/gnulib/blob/1039a5f2cee3cda1c11f64a5eb3a15b2e87cd2f0/m4/minmax.m4"""

load("//autoconf:autoconf_toolchain.bzl", autoconf = "autoconf_cache")
load("//autoconf:checks.bzl", "checks")

autoconf(
    name = "minmax",
    checks = [
        # Equivalent to gl_MINMAX macro from minmax.m4
        # On macOS, MIN/MAX are in sys/param.h
        checks.AC_TRY_COMPILE(
            code = """#include <sys/param.h>
int x = MIN (42, 17);
int y = MAX (42, 17);""",
            define = "HAVE_MINMAX_IN_SYS_PARAM_H",
            language = "c",
        ),
    ],
    visibility = ["//visibility:public"],
    deps = [
        # For test compatibility
        "//autoconf/macros/AC_CHECK_INCLUDES_DEFAULT",
    ],
)
