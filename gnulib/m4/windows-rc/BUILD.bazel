"""https://github.com/coreutils/gnulib/blob/1039a5f2cee3cda1c11f64a5eb3a15b2e87cd2f0/m4/windows-rc.m4"""

load("//autoconf:autoconf_toolchain.bzl", autoconf = "autoconf_cache")
load("//autoconf:checks.bzl", "checks")

# gl_WINDOWS_RC from windows-rc.m4
# AC_CHECK_TOOL([WINDRES], [windres]) on Windows; when not Windows, empty
autoconf(
    name = "windows-rc",
    checks = [
        checks.AC_SUBST("WINDRES", ""),
    ],
    visibility = ["//visibility:public"],
)
