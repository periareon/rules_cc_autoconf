"""https://github.com/coreutils/gnulib/blob/635dbdcf501d52d2e42daf6b44261af9ce2dfe38/m4/cosl.m4"""

load("//autoconf:autoconf.bzl", "autoconf")
load("//autoconf:macros.bzl", "macros")

autoconf(
    name = "cosl",
    checks = [
        # Check for cosl declaration (only for subst, not config.h)
        # The golden file shows HAVE_DECL_COSL should be undefined in config.h
        # Use internal define to avoid defining it in config.h
        macros.AC_CHECK_DECL(
            "cosl",
            define = "_gl_cv_have_decl_cosl",
            headers = ["math.h"],
        ),
    ],
    visibility = ["//visibility:public"],
    deps = [
        # AC_REQUIRE([gl_USE_SYSTEM_EXTENSIONS])
        "//gnulib/m4/extensions",
        "//gnulib/m4/cos",
        "//gnulib/m4/math_h",  # AC_REQUIRE([gl_MATH_H_DEFAULTS])
        "//gnulib/m4/sinl",
        "//gnulib/m4/mktime",  # Provides HAVE_DECL_ALARM (needed for test compatibility)
        "//autoconf/macros/AC_CHECK_INCLUDES_DEFAULT",
    ],
)
