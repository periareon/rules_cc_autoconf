"""https://github.com/coreutils/gnulib/blob/635dbdcf501d52d2e42daf6b44261af9ce2dfe38/m4/locale-ar.m4"""

load("//autoconf:autoconf.bzl", "autoconf")

autoconf(
    name = "locale-ar",
    checks = [
        # gt_LOCALE_AR macro from locale-ar.m4 determines the name of an Arabic
        # locale with traditional encoding. This involves runtime locale testing
        # that is complex to replicate in Bazel.
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Note: AC_REQUIRE([AC_CANONICAL_HOST]) and AC_REQUIRE([AM_LANGINFO_CODESET])
        # are standard autoconf macros (AM_LANGINFO_CODESET is from gettext)
    ],
)
