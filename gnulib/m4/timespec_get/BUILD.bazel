"""https://github.com/coreutils/gnulib/blob/635dbdcf501d52d2e42daf6b44261af9ce2dfe38/m4/timespec_get.m4"""

load("//autoconf:autoconf.bzl", "autoconf")
load("//autoconf:macros.bzl", "macros")

# gl_FUNC_TIMESPEC_GET from timespec_get.m4
autoconf(
    name = "timespec_get",
    checks = [
        # gl_CHECK_FUNC_TIMESPEC_GET is provided by gettime (via time_h)
        # which already checks for HAVE_TIMESPEC_GET
        # We don't need to check again here
    ],
    visibility = ["//visibility:public"],
    deps = [
        # AC_REQUIRE([gl_USE_SYSTEM_EXTENSIONS])
        "//gnulib/m4/extensions",
        # Standard headers are expected by tests (matching GNU autoconf behavior)
        "//autoconf/macros/AC_CHECK_INCLUDES_DEFAULT",
        # AC_REQUIRE([gl_TIME_H_DEFAULTS]) - provides gl_CHECK_FUNC_TIMESPEC_GET via gettime
        "//gnulib/m4/time_h",
    ],
)
