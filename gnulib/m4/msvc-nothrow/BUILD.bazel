"""https://github.com/coreutils/gnulib/blob/1039a5f2cee3cda1c11f64a5eb3a15b2e87cd2f0/m4/msvc-nothrow.m4"""

load("//autoconf:autoconf_toolchain.bzl", autoconf = "autoconf_cache")

autoconf(
    name = "msvc-nothrow",
    checks = [
        # gl_MSVC_NOTHROW macro from msvc-nothrow.m4 only has prerequisites
        # No actual checks needed
    ],
    visibility = ["//visibility:public"],
    deps = [
        "//gnulib/m4/msvc-inval",  # AC_REQUIRE([gl_MSVC_INVAL])
    ],
)
