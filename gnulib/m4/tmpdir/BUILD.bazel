"""https://github.com/coreutils/gnulib/blob/1039a5f2cee3cda1c11f64a5eb3a15b2e87cd2f0/m4/tmpdir.m4"""

load("//autoconf:autoconf_toolchain.bzl", autoconf = "autoconf_cache")

# gt_TMPDIR macro from tmpdir.m4
# This macro only defines prerequisites for lib/tmpdir.c and has no checks.
autoconf(
    name = "tmpdir",
    checks = [
        # No checks - the macro body is just `:` (no-op)
    ],
    visibility = ["//visibility:public"],
)
