load("//gnulib/tests/compatibility:gnu_gnulib_diff_test_suite.bzl", "gnu_gnulib_diff_test_suite")

gnu_gnulib_diff_test_suite(
    name = "wint_t_test",
    # Bazel autoconf target that implements this m4 module
    bazel_autoconf_target = "//gnulib/m4/wint_t:with_defaults",
    # Templates
    config_h_in = "config.h.in",
    configure_ac = "configure.ac",
    # Golden files (expected output)
    golden_config_h = "golden_config.h.in",
    golden_subst_h = "golden_subst.h.in",
    # All m4 files needed to run autoconf
    m4_files = ["@gnulib//:all_m4"],
    subst_h_in = "subst.h.in",
    # Compile test
    test_c = "test_wint_t.c",
)
