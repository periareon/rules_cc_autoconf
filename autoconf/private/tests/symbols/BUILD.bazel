load("@rules_cc//cc:cc_test.bzl", "cc_test")
load("//autoconf:autoconf.bzl", "autoconf")
load("//autoconf:autoconf_hdr.bzl", "autoconf_hdr")
load("//autoconf:macros.bzl", "macros")

autoconf(
    name = "autoconf",
    package_name = "test_symbols",
    checks = [
        macros.AC_CHECK_SYMBOL(
            "NULL",
            code = "#include <stddef.h>",
        ),
        macros.AC_CHECK_SYMBOL("__linux__"),
        macros.AC_CHECK_SYMBOL("NONEXISTENT_SYMBOL"),
    ],
    package_version = "1.0.0",
)

autoconf_hdr(
    name = "config",
    out = "config.h",
    autoconf = ":autoconf",
    template = "config.h.in",
)

# Platform-dependent symbols, so only cc_test
cc_test(
    name = "test_symbols",
    srcs = [
        "test_symbols.c",
        ":config.h",
    ],
)
