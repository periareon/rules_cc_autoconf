"""AC_TYPE_OFF_T - Autoconf builtin macro

Checks if off_t type is defined and its size.

Defines:
  - off_t (if not defined, defined to long)
  - SIZEOF_OFF_T (size of off_t)
"""

load("//autoconf:autoconf.bzl", "autoconf")
load("//autoconf:checks.bzl", "checks")

autoconf(
    name = "AC_TYPE_OFF_T",
    checks = [
        checks.AC_CHECK_TYPE("off_t"),
        checks.AC_CHECK_SIZEOF("off_t"),
    ],
    visibility = ["//visibility:public"],
)
